import type { Client } from 'pg';

/**
 * Seed stubs — expanded in later stories as domain entities are added.
 * E2+ stories will add seedDrivers(), seedRides(), etc. here.
 */

export async function seedPilotCity(_client: Client): Promise<void> {
  // No domain tables exist yet — placeholder for E1.S7+.
}
