# Place this as .env next to docker-compose.prod.yml (or at /opt/rcab/compose/.env)
# These vars are interpolated by Docker Compose itself (not passed into containers).
REDIS_PASSWORD=<strong-password>
