{
  "name": "@rcab/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "dev": "ts-node src/main.ts",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@rcab/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "vitest": "^3.2.2"
  }
}
